# Shell scripts must stay LF: CRLF breaks bash on Git Bash and on the
# EC2 instance where deploy/ scripts are executed.
*.sh text eol=lf
Dockerfile text eol=lf
deploy/Caddyfile text eol=lf
deploy/docker-compose.yml text eol=lf
