/**
 * Shared form-input styling. One source of truth so fields look identical on
 * every page — add layout utilities (w-full, mt-1) at the call site.
 */
export const inputClassName =
  "rounded-md border border-gray-300 px-3 py-2 text-sm text-gray-900 focus:border-gray-900 focus:outline-none";
