{
  "name": "job-tracker-reminders",
  "version": "0.1.0",
  "private": true,
  "description": "Scheduled Lambda that emails each user a digest of follow-ups due and applications not yet applied to",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-sesv2": "^3.1110.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.21.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.10"
  }
}
